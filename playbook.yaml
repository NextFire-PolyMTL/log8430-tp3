---
- name: Setup Environment
  hosts: all
  tags: setup
  become: true
  roles:
    - kubernetes

- name: Run Benchmarks
  hosts: all
  tags: benchmarks
  become: true
  vars_files: variables.yaml
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  roles:
    - benchmarks
